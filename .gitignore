# Admin credentials — never commit
.env

# Python virtualenv — always create locally, never copy between machines/paths
.venv

# Runtime data — generated on first start, not part of the repo
data/

# macOS
.DS_Store

# Python bytecode
__pycache__/
*.pyc

# Local SSL certificate (generated by setup-ssl.sh, never committed)
ssl/
